<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smhrd.enter.googlemaptest">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Googlemaptest">
        <activity android:name=".Z_Settings"></activity>
        <activity android:name=".Z_ProfileManage" />
        <activity android:name=".Z_MyPage" />
        <activity android:name=".Y_UsageDetail_Request" />
        <activity android:name=".Y_UsageDetail_Delivery" />
        <activity android:name=".Y_Like_List" />
        <activity android:name=".Y_License_Enroll" />
        <activity android:name=".C_Delivery_Board" />
        <activity android:name=".C_Delivery_Address" />
        <activity android:name=".C_Delivery" />
        <activity android:name=".B_Request_Board" />
        <activity android:name=".B_Request_Address" />
        <activity android:name=".B_Request" />
        <activity android:name=".A_Login" />
        <activity android:name=".A_Join" />
        <activity android:name=".A_Enter" />
        <activity android:name=".MainActivity2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyA729UrxlCtxH869qiIAJj9Ve6fDXnU_YA" />
    </application>

</manifest>